/**
 * S3 — Goal selection + projected timeline. Placeholder (FD.md §2 S3).
 */
export default function OnboardingGoalPage() {
  return (
    <main style={{ padding: "calc(var(--safe-top) + 20px) 20px 40px" }}>
      <h1 style={{ fontSize: 28, fontWeight: 800 }}>What&rsquo;s your goal?</h1>
      <p style={{ color: "var(--ink-dim)" }}>Placeholder.</p>
    </main>
  );
}
