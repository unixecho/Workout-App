/**
 * S2 — Body stats (age/height/weight, unit toggle). Placeholder (FD.md §2 S2).
 */
export default function OnboardingBodyStatsPage() {
  return (
    <main style={{ padding: "calc(var(--safe-top) + 20px) 20px 40px" }}>
      <h1 style={{ fontSize: 28, fontWeight: 800 }}>Tell us about you</h1>
      <p style={{ color: "var(--ink-dim)" }}>Placeholder.</p>
    </main>
  );
}
