/**
 * S1 — Profile (handle, display name, avatar). Placeholder (FD.md §2 S1).
 */
export default function OnboardingProfilePage() {
  return (
    <main style={{ padding: "calc(var(--safe-top) + 20px) 20px 40px" }}>
      <h1 style={{ fontSize: 28, fontWeight: 800 }}>Set up your profile</h1>
      <p style={{ color: "var(--ink-dim)" }}>Placeholder.</p>
    </main>
  );
}
