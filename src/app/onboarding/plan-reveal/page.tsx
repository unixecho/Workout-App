/**
 * S5 — Plan reveal (generated week, Accept/Customize). Placeholder (FD.md §2 S5).
 */
export default function OnboardingPlanRevealPage() {
  return (
    <main style={{ padding: "calc(var(--safe-top) + 20px) 20px 40px" }}>
      <h1 style={{ fontSize: 28, fontWeight: 800 }}>Your week is ready</h1>
      <p style={{ color: "var(--ink-dim)" }}>Placeholder.</p>
    </main>
  );
}
