/**
 * Placeholder. Port from `claude design/profile and settings/Profile.dc.html` (FD.md §10).
 */
export default function ProfilePage() {
  return (
    <main style={{ padding: "calc(var(--safe-top) + 20px) 20px 40px" }}>
      <h1 style={{ fontSize: 34, fontWeight: 800, letterSpacing: "-0.02em" }}>Profile</h1>
      <p style={{ color: "var(--ink-dim)" }}>
        Placeholder — port from the approved mockup next.
      </p>
    </main>
  );
}
