/**
 * Placeholder. Port from `claude design/today/Today.dc.html` (FD.md §3).
 */
export default function TodayPage() {
  return (
    <main style={{ padding: "calc(var(--safe-top) + 20px) 20px 40px" }}>
      <p style={{ fontSize: 13, fontWeight: 600, color: "var(--blue)", textTransform: "uppercase" }}>
        Today
      </p>
      <h1 style={{ fontSize: 34, fontWeight: 800, letterSpacing: "-0.02em" }}>
        Let&rsquo;s go
      </h1>
      <p style={{ color: "var(--ink-dim)" }}>
        Placeholder — port from the approved mockup next.
      </p>
    </main>
  );
}
