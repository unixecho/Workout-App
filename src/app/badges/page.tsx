/**
 * Badges — pushed screen reached from Stats (FD.md §9). Placeholder.
 * Port from `claude design/badges/Badges.dc.html`.
 */
export default function BadgesPage() {
  return (
    <main style={{ padding: "calc(var(--safe-top) + 20px) 20px 40px" }}>
      <h1 style={{ fontSize: 34, fontWeight: 800, letterSpacing: "-0.02em" }}>
        Badges
      </h1>
      <p style={{ color: "var(--ink-dim)" }}>
        Placeholder — port from the approved mockup next.
      </p>
    </main>
  );
}
