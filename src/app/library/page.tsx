/**
 * Exercise Library — browse view. Not a tab; reached from Plan header,
 * Workout Player rows, and the Session Editor picker (FD.md §7). Placeholder.
 * Port from `claude design/exercise library/Exercise Library.dc.html`.
 */
export default function LibraryPage() {
  return (
    <main style={{ padding: "calc(var(--safe-top) + 20px) 20px 40px" }}>
      <h1 style={{ fontSize: 34, fontWeight: 800, letterSpacing: "-0.02em" }}>
        Exercise Library
      </h1>
      <p style={{ color: "var(--ink-dim)" }}>
        Placeholder — port from the approved mockup next.
      </p>
    </main>
  );
}
